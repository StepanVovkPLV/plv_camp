How to install

1. Clone the project from github
2. Run `aclocal` in project directory
3. Run `autoconf` in project directory
4. Run `automake --add-missing` in project directory
5. Run `./configure` in the project directory
6. Run `sudo make install` in the project directory

How to use:

	run the following command in the project directory ./helloworld


How to uninstall:
	run the following command in the project directory `make clean-all`